import 'package:flutter/material.dart';

void main() {
  runApp(
    Center(
      child: Text(
        'data',
        style: TextStyle(
            fontSize: 74, fontWeight: FontWeight.bold, color: Colors.red),
        textDirection: TextDirection.ltr,
      ),
    ),
  );
}
